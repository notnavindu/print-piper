{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Capability for the main window (endpoints + logs)",
  "windows": ["main"],
  "permissions": ["core:default"]
}
