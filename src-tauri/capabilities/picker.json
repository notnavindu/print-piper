{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "picker-window",
  "description": "Capability for the picker window (preview + send)",
  "windows": ["picker"],
  "permissions": [
    "core:default",
    "core:window:allow-hide",
    "core:window:allow-show",
    "core:window:allow-set-focus"
  ]
}
